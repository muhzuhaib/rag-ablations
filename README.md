# rag-ablations

**Most RAG projects claim they improved retrieval. Almost none say what they improved it over.**

This is a retrieval benchmark that always states its baseline. Every design choice — chunking,
sparse vs dense vs hybrid, reranking — is measured against classical BM25 on public corpora with
real relevance judgments, and every number in this README is regenerated by one command.

It runs on a laptop CPU in minutes, with no API keys and nothing to pay for, so anyone reading this
can reproduce the table rather than take it on trust.

## Results

SciFact — 5,183 documents, 300 test queries.

| System | nDCG@10 | Recall@100 | Index (s) | ms/query |
|---|---|---|---|---|
| BM25 (stemmed) | 0.6862 | 0.9209 | 2.4 | 1.4 |
| BM25 (no stemming) | 0.6663 | 0.8859 | 0.9 | 1.1 |

NFCorpus — 3,633 documents, 323 test queries.

| System | nDCG@10 | Recall@100 | Index (s) | ms/query |
|---|---|---|---|---|
| BM25 (stemmed) | 0.3219 | 0.2464 | 2.7 | 0.5 |
| BM25 (no stemming) | 0.3072 | 0.2349 | 0.8 | 0.4 |

**Is the baseline itself correct?** That question decides whether anything else here means
anything, so it is checked against published figures rather than asserted. The BEIR paper reports
Anserini BM25 at **0.665** on SciFact and **0.325** on NFCorpus. This implementation gets 0.686 and
0.322 — within a point of both, from an independent implementation. The baseline is sound.

Raw output, including the environment each run was produced on, is in [`results/`](results/).

## Run it

```bash
pip install -e ".[dev]"
python -m rag_ablations.benchmark --dataset scifact
```

The corpus downloads itself on first use (2.8 MB) and is cached in `data/`. No keys, no accounts.

## Design decisions

**Public corpora with relevance judgments, not a private document set.** The alternative — indexing
some documents and eyeballing whether the answers look good — cannot produce a number anyone can
check. BEIR's SciFact and NFCorpus ship human judgments and published baselines, which is what makes
the results falsifiable.

**BM25 as the baseline, written from scratch.** BM25 still beats dense retrieval on many
out-of-domain corpora, so it is a real opponent rather than a straw man. It is implemented here
instead of imported from `rank_bm25` because it is the load-bearing number in the project: `k1` and
`b` are benchmark variables, and a baseline nobody can inspect is a baseline nobody should believe.

**Metrics implemented directly rather than via `pytrec_eval`.** That package needs a C build
toolchain, which breaks "reproducible by anyone on a laptop". nDCG and recall are forty lines, and
[`tests/test_metrics.py`](tests/test_metrics.py) checks them against values computed by hand.

**Two analyzers reported, not one.** Published BM25 baselines use a stemming, stopword-removing
Lucene analyzer. Reporting only the naive tokeniser would have understated the baseline by two
points and quietly flattered everything measured against it; reporting only the stemmed one would
hide how much of the score comes from the analyzer. Both are in the table.

**Queries with no relevance judgments are skipped, not scored zero.** BEIR ships all queries
regardless of split. Scoring unjudged ones as failures would scale every system down by the same
constant — the ranking would survive but the absolute numbers would no longer be comparable to
published figures.

**Recall@100 reported alongside nDCG@10.** A reranker can only reorder what the first stage
retrieved, so first-stage recall is the ceiling on the whole pipeline. Without it, a reranking
result is uninterpretable.

**Dense retrieval is an optional install.** `pip install rag-ablations` should not pull 2 GB of
PyTorch on someone who wants the baseline and the metrics. Dense retrieval lives behind
`.[dense]`.

**`certifi` pinned for the corpus download.** Python does not use the Windows trust store and does
not chase AIA links for missing intermediate certificates, so the stdlib default fails on hosts
that `curl` and every browser accept. This was found on the first real download, not guessed at.

## What is not here yet

Dense and hybrid retrieval, reranking, chunking ablations, and the FastAPI service are in progress.
This README will only ever show numbers that `results/` can back up.

## License

MIT
